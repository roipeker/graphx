name: graphx
description: Flash a-like render API on CustomPainter to power-up your Flutter apps.
version: 1.0.0+5
homepage: https://github.com/roipeker/graphx

environment:
  sdk: '>=2.9.1 <3.0.0'

dependencies:
   flutter:
    sdk: flutter
   xml: ^4.5.1

dev_dependencies:
  pedantic: ^1.9.0
  flutter_test:
    sdk: flutter
  effective_dart: ^1.0.0